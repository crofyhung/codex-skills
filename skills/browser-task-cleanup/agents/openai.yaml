interface:
  display_name: "Browser Task Cleanup"
  short_description: "Keep browser-heavy ecommerce/admin tasks from leaving temporary tabs behind."
  default_prompt: "Use $browser-task-cleanup to record browser state, protect risky ecommerce/admin pages, and close temporary tabs after the task."
